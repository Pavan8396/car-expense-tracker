import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.carexpensetracker.app',
  appName: 'Car Expense Tracker',
  webDir: 'build'
};

export default config;
